export { buildExtractionPrompt, buildFactExtractionPrompt, IDENTITY_EXTRACTION_PROMPT, FACT_EXTRACTION_PROMPT } from "./prompts.js";
export { extractIdentityFromText, mergeIdentity, type LLMProvider, type ExtractionResult } from "./extractor.js";
