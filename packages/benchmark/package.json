{
  "name": "@arete/benchmark",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Benchmark suite for Arete identity extraction",
  "scripts": {
    "benchmark": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@arete/core": "*",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.6.0"
  }
}
